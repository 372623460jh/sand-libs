"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function _react() {
  const data = _interopRequireDefault(require("react"));

  _react = function _react() {
    return data;
  };

  return data;
}

var _isHashRoute = _interopRequireDefault(require("./isHashRoute"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * 生成需要跳转的页面
 * hash 路由使用 params
 * 正常路由使用 hash
 * @param pathName
 * @param slug
 */
const getGotoPathName = (pathName, slug, location = window.location) => {
  const search = location.search;
  const pathArray = [];

  if ((0, _isHashRoute.default)(location)) {
    pathArray.push(`?anchor=${slug}`);

    if (search) {
      pathArray.push(`&${search.replace('?', '')}`);
    } // 拼接原来的 search 防止丢失


    return pathArray.join('');
  }

  return `${pathName}${search}#${slug}`;
};

var _default = getGotoPathName;
exports.default = _default;