"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = exports.issueLink = void 0;

function _react() {
  const data = _interopRequireDefault(require("react"));

  _react = function _react() {
    return data;
  };

  return data;
}

var _getParams = _interopRequireDefault(require("./getParams"));

var _template = require("./template");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(Object(source), true).forEach(function (key) { _defineProperty(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

const ensureReact = deps => {
  if (!deps.react && !deps['react-dom']) {
    deps.react = 'latest';
    deps['react-dom'] = 'latest';
  } else if (!deps.react) {
    deps.react = deps['react-dom'];
  } else if (!deps['react-dom']) {
    deps['react-dom'] = deps.react;
  }
};

const issueLink = `/** This is an auto-generated demo by dumi
* if you think it is not working as expected,
* please report the issue at
* https://github.com/umijs/dumi/issues
**/
ReactDOM.render(
  <App />,
  document.getElementById('root')
);`;
exports.issueLink = issueLink;

var _default = ({
  files,
  deps,
  devDependencies,
  desc
}, config) => {
  if (!config) config = {};
  const _config = config,
        extraFiles = _config.extraFiles,
        extraDependencies = _config.extraDependencies,
        name = _config.name,
        _config$template = _config.template,
        template = _config$template === void 0 ? 'create-react-app' : _config$template;
  let _config2 = config,
      main = _config2.main;

  const dependencies = _objectSpread(_objectSpread({}, deps), extraDependencies);

  main = !main && template === 'create-react-app-typescript' ? 'index.tsx' : 'index.jsx';
  ensureReact(dependencies);

  const finalFiles = _objectSpread(_objectSpread({}, files), {}, {
    'package.json': {
      content: (0, _template.newpkgJSON)(dependencies, name, main, devDependencies || {}, desc)
    },
    'sandbox.config.json': {
      content: JSON.stringify({
        template
      })
    }
  }, extraFiles);

  const parameters = (0, _getParams.default)({
    files: finalFiles
  });
  return {
    files: finalFiles,
    dependencies,
    parameters
  };
};

exports.default = _default;